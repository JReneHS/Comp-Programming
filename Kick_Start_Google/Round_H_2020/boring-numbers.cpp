#include <bits/stdc++.h>

#define endl "\n"
#define _io ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);

using namespace std;

typedef long long int lli;

int main() { _io
    unsigned int t; cin>>t;
    short arr(18);
    while(t--){
        //un numero aburrido es aquel que tiene numeros pares en las posiciones 
        //pares y numeros impares en posiciones inmpares
        //hay que saber cuantos numeros aburridos hay entre L y R inclusivo
    }
    return 0;
}


